{
  SdkPlatform targetPlatform=toSdkPlatform(key.getTargetPlatform());
  try {
    return clientProvider.getClient().generateSdk(targetPlatform,key.getApplicationId(),key.getProfileSchemaVersion(),key.getConfigurationSchemaVersion(),key.getNotificationSchemaVersion(),key.getAefMapIds(),key.getLogSchemaVersion());
  }
 catch (  TException e) {
    throw Utils.handleException(e);
  }
}
