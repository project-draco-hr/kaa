{
  SandboxBuilder builder=SandboxBuilderFactory.createSandboxBuilder(basePath,boxType,osType,baseImageUrl,boxName,imageOutputFile,sshForwardPort,webAdminForwardPort);
  builder.buildSandboxImage();
}
