{
  int touchProgress=(int)Math.round(valuePerDegree() * angle);
  touchProgress=(touchProgress < 0) ? 0 : touchProgress;
  touchProgress=(touchProgress > mMax) ? mMax : touchProgress;
  return touchProgress;
}
