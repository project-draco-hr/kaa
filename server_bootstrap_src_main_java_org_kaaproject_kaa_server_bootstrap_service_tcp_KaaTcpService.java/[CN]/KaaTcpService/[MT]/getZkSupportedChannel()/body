{
  ZkSupportedChannel zkChannel=new ZkSupportedChannel(ZkChannelsUtils.getZkChannelTypeFromChanneltype(getChannelType()),false,getZkCommunicationParameters(),getChannelStatistics());
  return new SupportedChannel(zkChannel);
}
