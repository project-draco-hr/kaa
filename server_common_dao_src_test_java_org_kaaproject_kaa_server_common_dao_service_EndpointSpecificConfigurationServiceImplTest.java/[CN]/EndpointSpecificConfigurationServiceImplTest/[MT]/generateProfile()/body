{
  EndpointProfileDto profile=new EndpointProfileDto();
  profile.setEndpointKeyHash(DECODED_KEY);
  profile.setConfigurationVersion(CONF_VERSION);
  profile.setApplicationId(APP_ID);
  return profile;
}
