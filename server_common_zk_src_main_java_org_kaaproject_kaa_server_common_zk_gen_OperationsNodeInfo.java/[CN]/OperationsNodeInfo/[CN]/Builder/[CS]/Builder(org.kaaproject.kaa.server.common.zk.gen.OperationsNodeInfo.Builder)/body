{
  super(other);
  if (isValidValue(fields()[0],other.connectionInfo)) {
    this.connectionInfo=data().deepCopy(fields()[0].schema(),other.connectionInfo);
    fieldSetFlags()[0]=true;
  }
  if (isValidValue(fields()[1],other.processedRequestCount)) {
    this.processedRequestCount=data().deepCopy(fields()[1].schema(),other.processedRequestCount);
    fieldSetFlags()[1]=true;
  }
  if (isValidValue(fields()[2],other.registeredUsersCount)) {
    this.registeredUsersCount=data().deepCopy(fields()[2].schema(),other.registeredUsersCount);
    fieldSetFlags()[2]=true;
  }
  if (isValidValue(fields()[3],other.deltaCalculationCount)) {
    this.deltaCalculationCount=data().deepCopy(fields()[3].schema(),other.deltaCalculationCount);
    fieldSetFlags()[3]=true;
  }
  if (isValidValue(fields()[4],other.timeStarted)) {
    this.timeStarted=data().deepCopy(fields()[4].schema(),other.timeStarted);
    fieldSetFlags()[4]=true;
  }
}
