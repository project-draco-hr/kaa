{
  String sdkToken=sessionInfo.getSdkToken();
  return new SessionInfo(sessionInfo.getUuid(),sessionInfo.getPlatformId(),sessionInfo.getCtx(),sessionInfo.getChannelType(),sessionInfo.getCipherPair(),sessionInfo.getKey(),getAppToken(sdkToken),sdkToken,sessionInfo.getKeepAlive(),sessionInfo.isEncrypted());
}
