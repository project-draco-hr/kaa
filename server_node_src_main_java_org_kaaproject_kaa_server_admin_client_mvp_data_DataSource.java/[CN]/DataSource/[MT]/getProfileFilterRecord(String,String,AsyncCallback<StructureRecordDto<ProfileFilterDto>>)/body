{
  rpcService.getProfileFilterRecord(schemaId,endpointGroupId,new DataCallback<StructureRecordDto<ProfileFilterDto>>(callback){
    @Override protected void onResult(    StructureRecordDto<ProfileFilterDto> result){
    }
  }
);
}
