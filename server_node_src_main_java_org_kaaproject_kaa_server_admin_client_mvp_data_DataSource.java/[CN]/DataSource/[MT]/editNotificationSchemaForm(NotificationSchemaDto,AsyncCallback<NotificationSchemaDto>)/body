{
  rpcService.editNotificationSchemaForm(notificationSchema,new DataCallback<NotificationSchemaDto>(callback){
    @Override protected void onResult(    NotificationSchemaDto result){
      eventBus.fireEvent(new DataEvent(NotificationSchemaDto.class));
    }
  }
);
}
