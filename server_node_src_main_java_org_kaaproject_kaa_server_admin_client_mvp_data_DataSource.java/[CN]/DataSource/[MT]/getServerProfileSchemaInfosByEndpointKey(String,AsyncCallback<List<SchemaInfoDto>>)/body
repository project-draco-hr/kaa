{
  profileRpcService.getServerProfileSchemaInfosByEndpointKey(endpointKeyHash,new DataCallback<List<SchemaInfoDto>>(callback){
    @Override protected void onResult(    List<SchemaInfoDto> result){
    }
  }
);
}
