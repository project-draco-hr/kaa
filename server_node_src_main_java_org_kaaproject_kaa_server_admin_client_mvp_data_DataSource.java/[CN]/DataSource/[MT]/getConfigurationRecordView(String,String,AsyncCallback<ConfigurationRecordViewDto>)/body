{
  configurationRpcService.getConfigurationRecordView(schemaId,endpointGroupId,new DataCallback<ConfigurationRecordViewDto>(callback){
    @Override protected void onResult(    ConfigurationRecordViewDto result){
    }
  }
);
}
