{
  rpcService.getConfigurationRecordForm(schemaId,endpointGroupId,new DataCallback<StructureRecordDto<ConfigurationRecordFormDto>>(callback){
    @Override protected void onResult(    StructureRecordDto<ConfigurationRecordFormDto> result){
    }
  }
);
}
