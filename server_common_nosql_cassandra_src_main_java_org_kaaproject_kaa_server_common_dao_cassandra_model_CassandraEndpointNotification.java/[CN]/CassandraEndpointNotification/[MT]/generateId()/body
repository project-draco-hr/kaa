{
  String id=null;
  if (endpointKeyHash != null) {
    StringBuilder builder=new StringBuilder(Bytes.toHexString(endpointKeyHash));
    builder.append(KEY_DELIMITER).append(seqNum);
    id=builder.toString();
  }
  return id;
}
