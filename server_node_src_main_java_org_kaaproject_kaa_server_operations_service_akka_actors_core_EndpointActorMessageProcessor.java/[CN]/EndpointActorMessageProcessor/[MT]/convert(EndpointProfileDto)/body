{
  return new EndpointProfileDataDto(profileDto.getId(),endpointKey,profileDto.getProfileVersion(),profileDto.getClientProfileBody(),profileDto.getServerProfileCtlSchemaId(),profileDto.getServerProfileBody());
}
