{
  if (oldRequest == null) {
    return newRequest;
  }
 else {
    if (oldRequest.getAppStateSeqNumber() < newRequest.getAppStateSeqNumber() || (newRequest.getAcceptedUnicastNotifications() != null && newRequest.getAcceptedUnicastNotifications().size() > 0) || (newRequest.getSubscriptionCommands() != null && newRequest.getSubscriptionCommands().size() > 0)) {
      return newRequest;
    }
 else {
      return null;
    }
  }
}
