{
  SessionHistory track=sessions.get(uuid);
  if (track != null) {
    track.sessionClose();
synchronized (onlineSessionsSync) {
      if (onlineSessions > 0)       onlineSessions--;
    }
  }
  LOG.debug("StatisticsService {}, session {} closed",channelType.toString(),uuid.toString());
}
