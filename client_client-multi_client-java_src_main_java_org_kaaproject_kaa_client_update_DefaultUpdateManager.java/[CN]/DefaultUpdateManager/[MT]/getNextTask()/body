{
synchronized (notificationCommands) {
    sentNotificationCommands.addAll(notificationCommands);
    notificationCommands.clear();
  }
  return commandFactory.createLongPollCommand(transport,TIMEOUT,acceptedUnicastNotificationIds,sentNotificationCommands);
}
