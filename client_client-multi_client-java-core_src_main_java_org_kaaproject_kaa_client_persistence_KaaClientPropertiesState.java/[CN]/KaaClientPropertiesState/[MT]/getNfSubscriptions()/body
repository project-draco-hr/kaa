{
  HashMap<Long,Integer> subscriptions=new HashMap<>();
  for (  Map.Entry<Long,TopicSubscriptionInfo> cursor : nfSubscriptions.entrySet()) {
    subscriptions.put(cursor.getKey(),cursor.getValue().getSeqNumber());
  }
  return subscriptions;
}
