{
  final int prime=31;
  int result=1;
  result=prime * result + ((projectDataType == null) ? 0 : projectDataType.hashCode());
  result=prime * result + ((projectId == null) ? 0 : projectId.hashCode());
  return result;
}
