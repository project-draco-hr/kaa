{
  RedirectSyncResponse redirectSyncResponse=new RedirectSyncResponse(redirection.getDnsName());
  SyncResponse response=new SyncResponse(0,SyncResponseStatus.REDIRECT,null,null,redirectSyncResponse);
  encodeAndReply(new NettyDecodedResponseMessage(message.getHandlerUuid(),message.getChannelContext(),message.getCommand(),response));
}
