{
  List<Object> list=new ArrayList<Object>();
  boolean present_pageLink=true && (isSetPageLink());
  list.add(present_pageLink);
  if (present_pageLink)   list.add(pageLink);
  return list.hashCode();
}
