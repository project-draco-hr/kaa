{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_tenant=true && (isSetTenant());
  builder.append(present_tenant);
  if (present_tenant)   builder.append(tenant);
  return builder.toHashCode();
}
