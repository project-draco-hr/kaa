{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_topic=true && (isSetTopic());
  builder.append(present_topic);
  if (present_topic)   builder.append(topic);
  return builder.toHashCode();
}
