{
  List<Object> list=new ArrayList<Object>();
  boolean present_endpointUserId=true && (isSetEndpointUserId());
  list.add(present_endpointUserId);
  if (present_endpointUserId)   list.add(endpointUserId);
  return list.hashCode();
}
