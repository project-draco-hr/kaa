{
  List<Object> list=new ArrayList<Object>();
  boolean present_configurationId=true && (isSetConfigurationId());
  list.add(present_configurationId);
  if (present_configurationId)   list.add(configurationId);
  boolean present_activatedUsername=true && (isSetActivatedUsername());
  list.add(present_activatedUsername);
  if (present_activatedUsername)   list.add(activatedUsername);
  return list.hashCode();
}
