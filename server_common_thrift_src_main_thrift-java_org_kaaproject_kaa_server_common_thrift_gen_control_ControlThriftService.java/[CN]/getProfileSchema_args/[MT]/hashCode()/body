{
  List<Object> list=new ArrayList<Object>();
  boolean present_profileSchemaId=true && (isSetProfileSchemaId());
  list.add(present_profileSchemaId);
  if (present_profileSchemaId)   list.add(profileSchemaId);
  return list.hashCode();
}
