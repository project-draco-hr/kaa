{
  List<Object> list=new ArrayList<Object>();
  boolean present_userVerifier=true && (isSetUserVerifier());
  list.add(present_userVerifier);
  if (present_userVerifier)   list.add(userVerifier);
  return list.hashCode();
}
