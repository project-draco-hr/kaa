{
switch (field) {
case APPLICATION_ID:
    if (value == null) {
      unsetApplicationId();
    }
 else {
      setApplicationId((String)value);
    }
  break;
case SCHEMA_VERSION:
if (value == null) {
  unsetSchemaVersion();
}
 else {
  setSchemaVersion((Integer)value);
}
break;
case RECORD_FILE:
if (value == null) {
unsetRecordFile();
}
 else {
setRecordFile((RecordFile)value);
}
break;
}
}
