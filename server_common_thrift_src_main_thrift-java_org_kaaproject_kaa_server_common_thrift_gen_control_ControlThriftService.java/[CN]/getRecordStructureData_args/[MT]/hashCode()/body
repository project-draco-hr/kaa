{
  List<Object> list=new ArrayList<Object>();
  boolean present_applicationId=true && (isSetApplicationId());
  list.add(present_applicationId);
  if (present_applicationId)   list.add(applicationId);
  boolean present_schemaVersion=true;
  list.add(present_schemaVersion);
  if (present_schemaVersion)   list.add(schemaVersion);
  boolean present_recordFile=true && (isSetRecordFile());
  list.add(present_recordFile);
  if (present_recordFile)   list.add(recordFile.getValue());
  return list.hashCode();
}
