{
  List<Object> list=new ArrayList<Object>();
  boolean present_profileSchema=true && (isSetProfileSchema());
  list.add(present_profileSchema);
  if (present_profileSchema)   list.add(profileSchema);
  return list.hashCode();
}
