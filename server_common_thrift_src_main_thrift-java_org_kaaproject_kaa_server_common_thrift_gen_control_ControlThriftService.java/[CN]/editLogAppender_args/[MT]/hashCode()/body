{
  List<Object> list=new ArrayList<Object>();
  boolean present_logAppender=true && (isSetLogAppender());
  list.add(present_logAppender);
  if (present_logAppender)   list.add(logAppender);
  return list.hashCode();
}
