{
  List<Object> list=new ArrayList<Object>();
  boolean present_userVerifierId=true && (isSetUserVerifierId());
  list.add(present_userVerifierId);
  if (present_userVerifierId)   list.add(userVerifierId);
  return list.hashCode();
}
