{
  List<Object> list=new ArrayList<Object>();
  boolean present_schemaId=true && (isSetSchemaId());
  list.add(present_schemaId);
  if (present_schemaId)   list.add(schemaId);
  boolean present_endpointGroupId=true && (isSetEndpointGroupId());
  list.add(present_endpointGroupId);
  if (present_endpointGroupId)   list.add(endpointGroupId);
  boolean present_deactivatedUsername=true && (isSetDeactivatedUsername());
  list.add(present_deactivatedUsername);
  if (present_deactivatedUsername)   list.add(deactivatedUsername);
  return list.hashCode();
}
