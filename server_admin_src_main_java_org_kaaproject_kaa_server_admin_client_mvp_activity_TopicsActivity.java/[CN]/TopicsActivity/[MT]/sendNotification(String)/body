{
  SendNotificationDialog.showSendNotificationDialog(applicationId,topicId,new AsyncCallback<SendNotificationDialog>(){
    @Override public void onFailure(    Throwable caught){
      Utils.handleException(caught,listView);
    }
    @Override public void onSuccess(    SendNotificationDialog result){
    }
  }
);
}
