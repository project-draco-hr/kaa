{
  ApplicationDto application=createApplication();
  ProfileSchemaDto profileSchema=createProfileSchema(application.getId());
  client.generateSdk(getSdkProfile(application.getId(),profileSchema.getMajorVersion(),2,2,null,0,null),org.kaaproject.kaa.server.common.thrift.gen.control.SdkPlatform.JAVA);
}
