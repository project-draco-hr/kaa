{
  return ThriftDtoConverter.toDataStruct(new SdkPropertiesDto(appId,configSchemaVersion,profileSchemaVersion,notificationSchemaVersion,logSchemaVersion,sdkPlatform,aefMapIds,defaultVerifierToken,null));
}
