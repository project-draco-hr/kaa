{
  EndpointGroupStateDto groupStateDto=new EndpointGroupStateDto();
  groupStateDto.setEndpointGroupId(idToString(endpointGroupId));
  groupStateDto.setProfileFilterId(idToString(profileFilterId));
  groupStateDto.setConfigurationId(idToString(configurationId));
  return groupStateDto;
}
