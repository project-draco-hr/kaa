{
  LOG.debug("Find inactive configuration by configuration schema id [{}] and endpoint group id [{}] ",schemaId,groupId);
  return findOne(query(where(SCHEMA_ID).is(new ObjectId(schemaId)).and(ENDPOINT_GROUP_ID).is(new ObjectId(groupId)).and(STATUS).is(INACTIVE.name())));
}
