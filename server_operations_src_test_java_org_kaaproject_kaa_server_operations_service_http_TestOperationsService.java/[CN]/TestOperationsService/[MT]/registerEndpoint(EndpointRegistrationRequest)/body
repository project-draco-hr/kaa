{
  OperationsHttpServerIT.EndpointRegisterTestSetRequestReceived(request.getVersionInfo().getConfigVersion(),request);
  SyncResponse response=generateSyncResponse();
  OperationsHttpServerIT.EndpointRegisterTestSetResponseSent(request.getVersionInfo().getConfigVersion(),response);
  SyncResponseHolder holder=new SyncResponseHolder(response);
  return holder;
}
