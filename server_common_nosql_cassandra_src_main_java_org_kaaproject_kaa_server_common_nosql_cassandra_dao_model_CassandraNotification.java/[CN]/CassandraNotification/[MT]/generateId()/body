{
  StringBuilder builder=new StringBuilder();
  builder.append(topicId).append(KEY_DELIMITER).append(type).append(KEY_DELIMITER).append(version).append(KEY_DELIMITER).append(seqNum);
  id=builder.toString();
}
