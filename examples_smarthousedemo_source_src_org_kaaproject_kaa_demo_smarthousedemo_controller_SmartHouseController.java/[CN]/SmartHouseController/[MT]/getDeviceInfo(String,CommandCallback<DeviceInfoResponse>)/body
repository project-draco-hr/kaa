{
  checkInited();
  new CommandAsyncTask<String,DeviceInfoResponse>(callback){
    @Override protected DeviceInfoResponse executeCommand(    String... endpontKey) throws Throwable {
      return new GetDeviceInfoCommand(commandMap,devices,endpontKey[0]).execute(executor,DEFAULT_TASK_TIMEOUT);
    }
  }
.execute(endpontKey);
}
