{
  if (oldRequest == null) {
    return newRequest;
  }
 else {
    if (oldRequest.getAppStateSeqNumber() < newRequest.getAppStateSeqNumber() || (newRequest.getAcceptedUnicastNotifications() != null && !newRequest.getAcceptedUnicastNotifications().isEmpty()) || (newRequest.getSubscriptionCommands() != null && !newRequest.getSubscriptionCommands().isEmpty())) {
      return newRequest;
    }
 else {
      return null;
    }
  }
}
