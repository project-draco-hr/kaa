{
  closed=true;
  try {
    if (streamClient != null) {
      streamClient.close();
    }
  }
 catch (  IOException e) {
    LOG.error("Failed to close stream client: ",e);
  }
  streamClient=null;
  LOG.debug("Stopped Cdap log appender.");
}
