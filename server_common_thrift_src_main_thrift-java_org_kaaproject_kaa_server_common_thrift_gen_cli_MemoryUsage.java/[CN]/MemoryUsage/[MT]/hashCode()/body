{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_max=true;
  builder.append(present_max);
  if (present_max)   builder.append(max);
  boolean present_total=true;
  builder.append(present_total);
  if (present_total)   builder.append(total);
  boolean present_free=true;
  builder.append(present_free);
  if (present_free)   builder.append(free);
  return builder.toHashCode();
}
