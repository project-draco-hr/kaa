{
synchronized (btSync) {
    if (!BtConnected) {
      try {
        btSync.wait();
      }
 catch (      InterruptedException e) {
        LOG.error("WaitConnected() interuppted.",e);
      }
    }
  }
}
