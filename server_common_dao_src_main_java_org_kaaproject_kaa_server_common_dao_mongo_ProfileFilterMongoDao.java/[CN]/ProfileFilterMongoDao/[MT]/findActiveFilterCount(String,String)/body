{
  LOG.debug("Find latest active profile filter by profile schema id [{}] and group id [{}]",schemaId,groupId);
  return count(query(where(SCHEMA_ID).is(new ObjectId(schemaId)).and(ENDPOINT_GROUP_ID).is(new ObjectId(groupId)).and(STATUS).is(ACTIVE.name())));
}
