{
  LOG.debug("Find actual profile filters by endpoint group id [{}] ",endpointGroupId);
  return find(query(where(ENDPOINT_GROUP_ID).is(new ObjectId(endpointGroupId)).and(STATUS).ne(DEPRECATED.name())));
}
