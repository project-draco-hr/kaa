{
  if (name != null) {
    OperationsServer server=getServerByName(name);
    if (server != null) {
      notifyChannelManangerAboutServer(server);
    }
 else {
      serverToApply=name;
      transport.sync();
    }
  }
}
