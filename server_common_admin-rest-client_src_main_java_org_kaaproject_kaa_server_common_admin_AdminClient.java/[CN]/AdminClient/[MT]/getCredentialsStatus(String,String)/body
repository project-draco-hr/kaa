{
  return this.restTemplate.getForObject(restTemplate.getUrl() + "credentialsStatus?applicationToken={applicationToken}&credentialsId={credentialsId}",CredentialsStatus.class,applicationToken,credentialsId);
}
