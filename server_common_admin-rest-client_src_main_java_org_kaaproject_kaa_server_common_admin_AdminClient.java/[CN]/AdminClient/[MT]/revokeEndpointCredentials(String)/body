{
  MultiValueMap<String,Object> parameters=new LinkedMultiValueMap<>();
  parameters.add("endpointId",endpointId);
  this.restTemplate.postForLocation(restTemplate.getUrl() + "revokeEndpointCredentials",parameters);
}
