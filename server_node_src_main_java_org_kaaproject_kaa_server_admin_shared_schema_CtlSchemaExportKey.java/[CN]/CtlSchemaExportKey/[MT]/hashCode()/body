{
  final int prime=31;
  int result=1;
  result=prime * result + ((ctlSchemaId == null) ? 0 : ctlSchemaId.hashCode());
  result=prime * result + ((exportMethod == null) ? 0 : exportMethod.hashCode());
  return result;
}
