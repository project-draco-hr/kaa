{
  if (playbackInfo.getStatus() == PlaybackStatus.PAUSED) {
    String url=null;
    if (mCurrentSong != null) {
      url=mCurrentSong.getUrl();
    }
 else {
      url=mSongUrls.get(0);
    }
    playUrl(url);
  }
 else {
    pause();
  }
}
