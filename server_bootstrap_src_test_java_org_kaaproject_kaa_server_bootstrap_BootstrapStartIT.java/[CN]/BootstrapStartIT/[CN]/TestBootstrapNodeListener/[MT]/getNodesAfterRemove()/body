{
synchronized (sync) {
    if (nodeInfos.size() > 0) {
      try {
        sync.wait(10000);
      }
 catch (      InterruptedException e) {
        logger.error(e.toString(),e);
      }
    }
    return nodeInfos;
  }
}
