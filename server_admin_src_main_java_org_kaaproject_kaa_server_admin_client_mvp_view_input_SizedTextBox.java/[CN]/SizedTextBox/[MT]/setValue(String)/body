{
  if (editable) {
    text.setValue(value);
  }
 else {
    if (isEmpty(value)) {
      textLabel.setHTML("&nbsp;");
    }
 else {
      textLabel.setText(value);
    }
  }
  updateCharactersLabel();
}
