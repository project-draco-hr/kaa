{
  if (getSetField() == _Fields.HTTP_LP_PARAMS) {
    return (ThriftIpParameters)getFieldValue();
  }
 else {
    throw new RuntimeException("Cannot get field 'httpLpParams' because union is currently set to " + getFieldDesc(getSetField()).name);
  }
}
