{
switch (setField) {
case HTTP_PARAMS:
    if (value instanceof ThriftIpParameters) {
      break;
    }
  throw new ClassCastException("Was expecting value of type ThriftIpParameters for field 'httpParams', but got " + value.getClass().getSimpleName());
case HTTP_LP_PARAMS:
if (value instanceof ThriftIpParameters) {
  break;
}
throw new ClassCastException("Was expecting value of type ThriftIpParameters for field 'httpLpParams', but got " + value.getClass().getSimpleName());
default :
throw new IllegalArgumentException("Unknown field id " + setField);
}
}
