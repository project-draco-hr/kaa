{
  if (getSetField() == _Fields.HTTP_PARAMS) {
    return (ThriftIpParameters)getFieldValue();
  }
 else {
    throw new RuntimeException("Cannot get field 'httpParams' because union is currently set to " + getFieldDesc(getSetField()).name);
  }
}
