{
  validateObject(endpointConfigurationDto,"Can't save endpoint configuration object. Incorrect endpoint configuration object." + endpointConfigurationDto);
  return getDto(endpointConfigurationDao.save(new EndpointConfiguration(endpointConfigurationDto)));
}
