{
  validateHash(endpointProfileKeyHash,"Can't find client- and server-side endpoint profile body by key hash. Invalid key hash " + endpointProfileKeyHash);
  return endpointProfileDao.findBodyByKeyHash(endpointProfileKeyHash);
}
