{
  boolean correct=false;
  if (id != null) {
    if (StringUtils.isNotEmpty(id.getId()) && !ObjectId.isValid(id.getId())) {
      correct=false;
    }
 else {
      correct=true;
    }
  }
  return correct;
}
