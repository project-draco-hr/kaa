{
synchronized (sync) {
    try {
      int uc=updateCount;
      sync.wait(2000);
      if (uc == updateCount) {
        fail("Update wait failed, timeout exception");
      }
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
      fail("Update wait failed");
    }
  }
}
