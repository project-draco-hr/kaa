{
  LOG.info("Initializing user verifier with context {} and configuration {}",context,configuration);
  thredPool=new ThreadPoolExecutor(0,configuration.getMaxParallelConnections(),60L,TimeUnit.SECONDS,new LinkedBlockingQueue<Runnable>());
}
