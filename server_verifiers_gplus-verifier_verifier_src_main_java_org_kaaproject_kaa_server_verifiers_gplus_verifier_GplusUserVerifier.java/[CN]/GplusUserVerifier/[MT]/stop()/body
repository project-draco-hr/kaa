{
  threadPool.shutdown();
  try {
    conManager.shutdown();
    httpClient.close();
  }
 catch (  IOException e) {
    e.printStackTrace();
    LOG.debug("Http client failed to close",e);
  }
  LOG.info("user verifier stopped");
}
