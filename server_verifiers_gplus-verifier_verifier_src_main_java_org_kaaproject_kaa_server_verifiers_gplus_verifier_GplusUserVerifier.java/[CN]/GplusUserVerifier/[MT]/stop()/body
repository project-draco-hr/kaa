{
  threadPool.shutdown();
  try {
    conManager.shutdown();
    httpClient.close();
  }
 catch (  IOException e) {
    LOG.warn("Http client failed to close: ",e);
  }
  LOG.info("user verifier stopped");
}
