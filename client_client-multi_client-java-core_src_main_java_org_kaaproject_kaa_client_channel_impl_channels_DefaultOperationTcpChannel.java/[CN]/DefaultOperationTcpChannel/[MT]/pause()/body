{
  LOG.info("Pausing...");
  if (!isPaused) {
    isPaused=true;
    closeConnection();
    if (executor != null) {
      executor.shutdownNow();
      isReadTaskScheduled=false;
      executor=null;
    }
  }
}
