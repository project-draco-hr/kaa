{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_updates=true && (isSetUpdates());
  builder.append(present_updates);
  if (present_updates)   builder.append(updates);
  return builder.toHashCode();
}
