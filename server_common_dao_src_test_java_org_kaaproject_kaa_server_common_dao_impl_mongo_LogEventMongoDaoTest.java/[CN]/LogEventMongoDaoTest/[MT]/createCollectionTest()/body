{
  Assert.assertFalse(MongoDBTestRunner.getDB().collectionExists(COLLECTION_NAME));
  logEventDao.createCollection(COLLECTION_NAME);
  Assert.assertTrue(MongoDBTestRunner.getDB().collectionExists(COLLECTION_NAME));
}
