{
  List<CTLSchemaDto> appSchemas=ctlService.findCTLSchemasByTenantId(tenant.getId());
  Assert.assertEquals(getIdsDto(Arrays.asList(firstSchema,secondSchema,thirdSchema,fourthSchema,mainSchema,tenantSchema,appSchema)),getIdsDto(appSchemas));
}
