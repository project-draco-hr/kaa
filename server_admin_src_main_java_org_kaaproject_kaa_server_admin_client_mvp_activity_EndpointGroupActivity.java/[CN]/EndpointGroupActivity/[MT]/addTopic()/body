{
  AddTopicDialog.showAddTopicDialog(entityId,new AsyncCallback<AddTopicDialog>(){
    @Override public void onFailure(    Throwable caught){
      Utils.handleException(caught,detailsView);
    }
    @Override public void onSuccess(    AddTopicDialog result){
    }
  }
);
}
