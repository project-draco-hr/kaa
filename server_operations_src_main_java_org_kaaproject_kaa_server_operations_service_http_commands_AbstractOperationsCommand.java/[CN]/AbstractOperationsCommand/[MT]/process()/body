{
  decode();
  LOG.trace("CommandName: " + COMMAND_NAME + ": Process.. EndpointService resolved");
  response=processParsedRequest(request);
  LOG.trace("CommandName: " + COMMAND_NAME + ": Process.. DB request processed");
  encode(response);
}
