{
  final int prime=31;
  int result=1;
  result=prime * result + ((kaaSinkKey == null) ? 0 : kaaSinkKey.hashCode());
  result=prime * result + ((rootPath == null) ? 0 : rootPath.hashCode());
  return result;
}
