{
  closed=true;
  if (logger != null) {
    try {
      logger.close();
    }
 catch (    IOException e) {
      LOG.warn("IO Exception");
    }
  }
  LOG.debug("Stoped filesystem log appender.");
}
