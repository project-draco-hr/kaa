{
  final int prime=31;
  int result=1;
  result=prime * result + ((endpontKey == null) ? 0 : endpontKey.hashCode());
  result=prime * result + ((fqn == null) ? 0 : fqn.hashCode());
  return result;
}
