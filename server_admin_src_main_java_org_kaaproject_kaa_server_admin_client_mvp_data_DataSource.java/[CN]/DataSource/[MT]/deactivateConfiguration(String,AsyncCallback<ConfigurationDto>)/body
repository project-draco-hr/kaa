{
  rpcService.deactivateConfiguration(configurationId,new DataCallback<ConfigurationDto>(callback){
    @Override protected void onResult(    ConfigurationDto result){
      eventBus.fireEvent(new DataEvent(ConfigurationDto.class));
    }
  }
);
}
