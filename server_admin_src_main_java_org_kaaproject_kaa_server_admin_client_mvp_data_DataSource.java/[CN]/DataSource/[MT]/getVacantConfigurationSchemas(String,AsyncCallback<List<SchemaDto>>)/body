{
  rpcService.getVacantConfigurationSchemasByEndpointGroupId(endpointGroupId,new DataCallback<List<SchemaDto>>(callback){
    @Override protected void onResult(    List<SchemaDto> result){
    }
  }
);
}
