{
  rpcService.getConfigurationRecord(schemaId,endpointGroupId,new DataCallback<StructureRecordDto<ConfigurationDto>>(callback){
    @Override protected void onResult(    StructureRecordDto<ConfigurationDto> result){
    }
  }
);
}
