{
  rpcService.editConfigurationSchema(configurationSchema,fileItemName,new DataCallback<ConfigurationSchemaDto>(callback){
    @Override protected void onResult(    ConfigurationSchemaDto result){
      eventBus.fireEvent(new DataEvent(ConfigurationSchemaDto.class));
    }
  }
);
}
