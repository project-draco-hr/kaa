{
  rpcService.getConfigurationRecordsByEndpointGroupId(endpointGroupId,includeDeprecated,new DataCallback<List<StructureRecordDto<ConfigurationDto>>>(callback){
    @Override protected void onResult(    List<StructureRecordDto<ConfigurationDto>> result){
    }
  }
);
}
