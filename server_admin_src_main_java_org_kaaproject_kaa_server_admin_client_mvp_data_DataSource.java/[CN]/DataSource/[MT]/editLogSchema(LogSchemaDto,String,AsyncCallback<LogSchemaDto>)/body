{
  rpcService.editLogSchema(logSchema,fileItemName,new DataCallback<LogSchemaDto>(callback){
    @Override protected void onResult(    LogSchemaDto result){
      eventBus.fireEvent(new DataEvent(LogSchemaDto.class));
    }
  }
);
}
