{
  name=name.toUpperCase();
  for (  CTLSchemaScope scope : CTLSchemaScope.values()) {
    if (name.equals(scope.name())) {
      return scope;
    }
  }
  throw new IllegalArgumentException("Invalid CTL schema scope name!");
}
