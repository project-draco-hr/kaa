{
  super(org.kaaproject.kaa.common.endpoint.gen.LogSyncRequest.SCHEMA$);
  if (isValidValue(fields()[0],other.requestId)) {
    this.requestId=data().deepCopy(fields()[0].schema(),other.requestId);
    fieldSetFlags()[0]=true;
  }
  if (isValidValue(fields()[1],other.logEntries)) {
    this.logEntries=data().deepCopy(fields()[1].schema(),other.logEntries);
    fieldSetFlags()[1]=true;
  }
}
