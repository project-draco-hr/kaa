{
  super(org.kaaproject.kaa.common.bootstrap.gen.SupportedChannel.SCHEMA$);
  if (isValidValue(fields()[0],other.channelType)) {
    this.channelType=data().deepCopy(fields()[0].schema(),other.channelType);
    fieldSetFlags()[0]=true;
  }
  if (isValidValue(fields()[1],other.communicationParameters)) {
    this.communicationParameters=data().deepCopy(fields()[1].schema(),other.communicationParameters);
    fieldSetFlags()[1]=true;
  }
}
