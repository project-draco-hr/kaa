{
  List<Object> list=new ArrayList<Object>();
  boolean present_priority=true;
  list.add(present_priority);
  if (present_priority)   list.add(priority);
  boolean present_publicKey=true && (isSetPublicKey());
  list.add(present_publicKey);
  if (present_publicKey)   list.add(publicKey);
  return list.hashCode();
}
